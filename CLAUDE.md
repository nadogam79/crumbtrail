# CrumbTrail

## 컨셉
헨젤과 그레텔이 길을 잃지 않기 위해 빵 조각을 흘려두었던 것처럼,
사용자가 이동하며 남기는 위치 신호를 "빵 부스러기(breadcrumb)"로 보고,
이상 신호가 감지되면 주변 지인들에게 알리는 안전 귀가 서비스.

## 프로젝트 개요
사회적 약자(여성 1인 가구, 노약자, 아동·청소년, 장애인 등)의 야간 귀가 안전을
위한 서비스. 신고(경찰 등) 기반이 아니라, 등록된 지인 네트워크에게 알리고
지인들이 자율적으로 판단/대응하게 하는 방식.

## 핵심 메커니즘
1. **경로 설정**: 출발 전 예상 이동 루트/목적지 등록
2. **위치 추적**: 이동 중 실시간 위치 신호(breadcrumb) 수집
3. **이상 감지 (데드맨 스위치 방식)**: 아래 중 하나라도 해당 시 이상 신호로 판단
   - 루트에서 일정 거리 이상 이탈
   - 일정 시간 이상 위치 정지 또는 신호 끊김
   - 예상 도착 시간 초과했는데 미도착
   - 사용자가 주기적 "괜찮음" 체크인을 하지 않음
4. **알림 트리거**: 이상 신호 발생 시 등록된 지인들에게 현재 위치 + 상황 자동 전송

## 전체 흐름
```
[루트 설정 + 이동 시작]
        ↓
[위치 신호(breadcrumb) 모니터링]
        ↓
  이상 신호 감지 (이탈/정지/체크인 실패 등)
        ↓
[등록된 지인들에게 알림 발송] (위치 + 상황 정보)
        ↓
   ┌───────────────┴───────────────┐
   │                               │
[사용자가 "괜찮음" 공지]      [응답 없음 / 실제 위험]
   → 메신저에 false alarm 알림      → 지인들이 상황 계속 주시/연락
   → 상태 해제                      → 지인들이 자율 판단으로 조치 (연락, 직접 확인, 필요시 신고 등)
```

## 메신저 컴포넌트
- 이상 신호 발생 시 알림 받은 지인들이 모이는 채팅방/스레드 자동 생성
- 사용자가 "나 괜찮아, 오탐이었어"라고 올리면 → 전체에 해제 알림 + 상태 종료
- 지인이 여러 명일 때 누가 먼저 반응/움직이고 있는지 상태 공유 (중복 대응 방지)
- 사용자 응답 불가 시 참고 정보(마지막 위치, 이동 경로, 배터리 상태 등)를 같이 표시

## 설계상 장점
- 신고 기반이 아니므로 이상 감지 임계값을 다소 민감하게 잡아도 부담 적음
  (오탐이어도 사용자가 바로 해제 가능)
- 지인 네트워크가 1차 대응층, 최종 판단(신고 여부 등)은 사람이 함
  → 자동화로 인한 오판 리스크 감소

## 기술적으로 고려할 지점
- 오탐(false positive) 관리: 지하철 등 정상적인 신호 끊김/정지 상황과의 구분
- 백그라운드 위치 추적: 웹만으론 한계 있음, PWA 또는 네이티브 앱 권한 필요 가능성
- 배터리 소모 최적화
- 체크인 주기, 이탈 허용 범위 등을 사용자가 직접 조절 가능하게

## 개발 환경
- WSL(Ubuntu) 기반, Windows 파일시스템(`/mnt/c/Users/.../source/repos/`) 내 프로젝트로 진행
- Node.js는 nvm으로 관리, Claude Code는 npm 전역 설치로 사용 중

## 다음 단계 (TODO)
- [ ] 이상 감지 임계값(이탈 거리, 정지 시간, 체크인 주기 등) 구체적으로 설계
- [ ] 메신저/알림 기능 구현 방식 결정 (자체 구현 vs 외부 서비스 연동)
- [ ] 위치 추적 기술 스택 결정 (PWA vs 네이티브)
- [ ] 프로젝트 뼈대 스캐폴딩
